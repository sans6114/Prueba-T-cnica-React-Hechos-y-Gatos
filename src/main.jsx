import './index.css';

import React from 'react';

import ReactDOM from 'react-dom/client';

import { AppGatos } from './AppGatos.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AppGatos />
  </React.StrictMode>,
)
